# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Directory & Architectural Conventions

Within `apps/`:

- **`features/`**: Contains all assets, components, and modules specific **only** to this application.
- **`framework/`**: Contains all reusable components and modules designed to be shared and reused in future projects.
